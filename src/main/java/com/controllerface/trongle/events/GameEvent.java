package com.controllerface.trongle.events;

import com.juncture.alloy.events.EventType;

public enum GameEvent implements EventType
{
    MODE_SWiTCH,
    ENTITY_DESTROYED,
    ENEMY_COUNT_CHANGED,
}
